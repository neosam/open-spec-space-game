## 1. Create README

- [x] 1.1 Create `README.md` with game title, description, features list, GitHub Pages link, controls, native build instructions (nix develop + cargo run), WASM build instructions (trunk serve / trunk build --release), and dev environment setup
