## Readme

### Requirement: README Exists
The project SHALL have a `README.md` at the repository root containing a game description, a features list, controls reference, build instructions, and a link to the live deployment.

#### Scenario: README is present
- **WHEN** a user visits the repository
- **THEN** a `README.md` SHALL be visible with project information
