schema: spec-driven

context: |
  Tech stack: Rust (2024 edition), Bevy 0.18 (2D)
  Build: Nix dev environment (flake.nix)
  Architecture: ECS (Bevy systems/components/resources)

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
