[package]
name = "spacegame"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18", default-features = false, features = ["2d"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3"
